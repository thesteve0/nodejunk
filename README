# Steve's node

Steps

  rhc app create -a snode -t nodejs-0.6
  rhc app cartridge add -a snode -c mongodb-2.0

  git pull -s recursive -X theirs git@github.com:marekjelen/nodejunk.git

Example

  https://snode-mjelen.rhcloud.com/